## Important NumPy Mathematical Formulas
import numpy as np
values = np.array([2,4,6,8,10])

square_root = np.sqrt(values)
exponential = np.exp(values)
natural_log = np.log(values)
total_sum = np.sum(values)
cumulative_sum = np.cumsum(values)


print(f"Square Root: {square_root}")
print(f"Exponential: {exponential}")
print(f"Natural Log: {natural_log}")
print(f"Total Sum: {total_sum}")
print(f"Cumulative Sum: {cumulative_sum}")